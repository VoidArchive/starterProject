# Server Configuration
PORT=8080
ENV=development

# Database Connection
# Choose the appropriate connection string for your setup:

# Local native Postgres
DATABASE_URL=postgresql://USER:PASS@127.0.0.1:5432/DBNAME?sslmode=disable

# Docker on Linux (Postgres mapped to host port 5433)
# DATABASE_URL=postgresql://USER:PASS@172.17.0.1:5433/DBNAME?sslmode=disable

# Docker Desktop (macOS/Windows)
# DATABASE_URL=postgresql://USER:PASS@host.docker.internal:5433/DBNAME?sslmode=disable

# Railway or cloud hosting (TLS required)
# DATABASE_URL=postgresql://USER:PASS@HOST:PORT/DBNAME?sslmode=require

# Authentication
JWT_SECRET=your-secret-key-change-in-production
