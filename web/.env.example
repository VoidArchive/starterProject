# API Connection (public - exposed to browser)
PUBLIC_API_URL=http://localhost:8080

# Database (server-side only - for SvelteKit server routes if needed)
# Use the same connection string as your API
DATABASE_URL=postgresql://USER:PASS@127.0.0.1:5432/DBNAME?sslmode=disable
